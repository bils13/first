"use client"

import styled from "styled-components"

const HeaderContainer = styled.header`

`

export function Header(){
    return(
        <HeaderContainer>
            
        </HeaderContainer>
    )
}